# Each tuple represents two options: (text, result)
# Result can be numeric (points) or strings like "LANDFILL" or "COMPOST"

TRACK_1_QUESTIONS = [
    [("Grow all together", 10), ("Only corn", -10)],
    [("Truck", 10), ("Plane", -10)],
    [("Buy from grocery store", -10), ("Buy from farmers market", 10)],
    [("Store in fridge", -10), ("Store on counter", 10)],
    [("Throw leftovers", -10), ("Compost", 10)]
    
]
